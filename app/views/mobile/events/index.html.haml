.row
  -if @events.empty?
    .col-md-12 There aren't any events on this day yet.
-@events.each do |event|
  .row.mb-1
    -#
      .col-md-4
        =link_to(image_for_with_retina(event, "83x70#"), mobile_event_path(event))
    .col-md-12
      %h3.small-title
        =link_to event.title, mobile_event_path(event)
      %p.program-subtitle=event.time_string_with_dots
      %p.program-subtitle=event.venue