-[(@page_children || @page.children).first].each do |page|
  .row.mb-1
    -#
      .col-md-4
        =link_to(image_for_with_retina(page, "83x70#"), mobile_page_path(page, :permalink => false))
    .col-md-12
      %h3.small-title=link_to(page, mobile_page_path(page, :permalink => false))
