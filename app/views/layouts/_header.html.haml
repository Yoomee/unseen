.container
  #header.row
    .span8
      professionals
      public
    .span4
      .pull-right
        -if user_signed_in?
          =link_to(t(:logout).downcase, sign_out_path, :method => :delete)
        -else
          =link_to(t(:login).downcase, sign_in_path)
        =link_to("contact", Page.find_by_slug("contact") || "#")
        %i.icon-search.icon-white
        f
        t
    .span4
      %h1#site_logo
        %span
          unseen photo fair
        %span
        19-23 sept 2012
    .span8
      #nav.pull-right
        %ul.unstyled
          %li=link_to("home", root_path)
          %li=link_to("news", Page.find_by_slug("news") || "#")
          %li=link_to("fair", fair_path)
          %li=link_to("galleries", Page.find_by_slug("galleries") || "#")
          %li=link_to("blog", Page.find_by_slug("blog") || "#")
          %li=link_to("press", Page.find_by_slug("press") || "#")
          %li=link_to("about", Page.find_by_slug("about") || "#")