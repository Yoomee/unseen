.container

#intro.home_box.parallax-subnav{:class => next_home_background_class}
  .subnav-strip
    .container#community-nav
      .row
        .col-md-8
          .subnav-tabs
            %ul
              =li_with_active(current_page?(community_path), link_to('Start collecting', community_path, :class => 'subnav-tab'))
              =li_with_active(current_page?(community_people_path), link_to('People', community_people_path, :class => 'subnav-tab'))
              =li_with_active(current_page?(community_activity_path), link_to('Latest activity', community_activity_path, :class => 'subnav-tab'))
              -if current_user
                =li_with_active(current_page?(user_path(current_user)), link_to('My Profile', current_user, :class => 'subnav-tab'))
        .col-md-4
          -if current_user
            =link_to('Log out', sign_out_path,:method => :delete,:class => 'black-btn')
            =link_to('My settings', edit_user_path(current_user),:class => 'black-btn')
          -else
            =link_to('Log in', sign_in_path,:class => 'black-btn')
            =link_to('Register', sign_up_path,:class => 'black-btn')
