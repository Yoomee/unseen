$('.add-to-collection[data-photo-id="<%=@photo.id%>"]').replaceWith('<%=escape_javascript(add_to_collection_link(@photo))%>');
$('.add-to-collection[data-photo-id="<%=@photo.id%>"]').tooltip({placement:'bottom'});
